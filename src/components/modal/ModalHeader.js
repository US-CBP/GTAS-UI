import React from "react";
import RBModalHeader from "react-bootstrap/ModalHeader";
const ModalHeader = props => {
  return <RBModalHeader>{props.children}</RBModalHeader>;
};

export default ModalHeader;
