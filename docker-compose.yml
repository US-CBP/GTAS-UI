version: '3.7'
services:
  gtas-ui:
    image: wcogtas/gtas-ui:dev
    container_name: gtas-ui
    build:
      context: .
      dockerfile: dockerfile
    ports:
      - 80:80
    restart: always
    networks:
      - gtas-webapp-network
networks:
  gtas-webapp-network:
    attachable: true